= simple_form_for(@menu) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nombre
    = f.input :fecha

    #platillos
      = render @menu.platillos

  .form-actions
    = f.button :submit, 'Guardar menu'
